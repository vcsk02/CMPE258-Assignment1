include: package:flutter_lints/flutter.yaml

linter:
  rules:
    prefer_const_constructors: true
    prefer_final_locals: true
    avoid_print: false # Allowed for debugging agent logic
    use_build_context_synchronously: false
